// arithmetic (+ - * / % ** ++ --)
let width = 5;
let height = 2;

// let addition = width + height;
// let subtraction = width - height;
// let multiplication = width * height;
// let division = width / height;
// let remainder = width % height;
// let exponentiation = width ** height;
// let widthIncrement = width++;
// let heightDecrement = --height;

// assignment (+= -= *= /= %= **= <<= >>= >>>= &= |= ^=)

// width += 2; // width = width + 2;
// width -= 2;
// width *= 2;
// width /= 2;
// width %= 2;
// width **= 2;

// comparison (> >= < <= == === != !==)
let age = 15;

// logical (! && ||)

let balance = 3_450_000;
let hasCrime = false;
let isEligibleForLoan = balance > 1_000_000 && !hasCrime;

let isFemale = false;
let isApproved = isFemale || age >= 18;

// bitwise (& | ^ ~ << >> >>>)
let number = 5;
let value = 1;

// type checking (typeof instanceof)
let firstName = new String("Pouria");
console.log(firstName instanceof String);
